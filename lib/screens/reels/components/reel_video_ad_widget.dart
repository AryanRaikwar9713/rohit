import 'package:flutter/material.dart';

class ReelVideoAdWidget extends StatelessWidget {
  const ReelVideoAdWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
